// run this on the page with all the questions 

let radios = document.querySelectorAll("[type=radio]")

    for (let i = 1; i < 15; i++) {
        radios = document.querySelectorAll("[type=radio]")
        if (i == 1 || i % 2 == 0) {
            console.log(radios[i]);
            if (radios.item(i) === null) 
                break;
            else 
                radios.item(i).click();
        }
    }
    document.querySelectorAll("[type=button]").item(1).click()